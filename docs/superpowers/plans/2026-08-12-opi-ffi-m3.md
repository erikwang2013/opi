# OPI M3 FFI 实施计划（flutter_rust_bridge 绑定 + EngineController）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 flutter_rust_bridge 生成 opi-ffi 绑定，暴露 M1 Engine 门面全量 API（击键路径同步 + 加载异步），封装 EngineController（Riverpod），Dart 集成测试通过。

**Architecture:** 新增 `crates/opi-ffi`（薄壳：`api.rs` 定义 frb API 层 + 类型转换，内部持 `engine_core::Engine`，字典经 `engine_data::load_or_fallback` 加载）；`flutter/app` 为 frb 生成的 Flutter 应用（native-assets 后端，`flutter test` 在主机上直接跑 Dart 集成测试）；`lib/engine/engine_controller.dart` 用 Riverpod 的 ChangeNotifierProvider 封装绑定，作为 UI 单一状态源（M4 键盘接入用）。

**Tech Stack:** Rust（edition 2024）+ flutter_rust_bridge 2.x（crate + Dart 包 + codegen 工具）+ Flutter 3.44.2 / Dart 3.12.2（已装，linux 主机）+ flutter_riverpod。

---

## 关键事实（实现时直接使用）

- **Engine 门面**（`crates/engine-core/src/engine.rs`）：`Engine::new(dict: Box<dyn Dictionary>, symbols: SymbolEngine, learner_enabled: bool)`；方法见 Task 3 代码。
- **Dictionary 加载**：`engine_data::load_or_fallback(path: Option<&Path>) -> Result<Box<dyn Dictionary>, String>`；`engine_data::fallback_dict() -> MmapDictionary`。返回 `Box<dyn Dictionary>` 直接喂给 `Engine::new`。fallback 词库含 我/你/好/是 等 35 条。
- **类型**：`Candidate { text: String, kind: CandidateKind, score: u64 }`；`CandidateKind { Hanzi, English, Emoji, Symbol }`；`Mode { Pinyin, English, Number, Symbol }`（Default Pinyin）；`BlockId(pub u16)`；`Block { id, start: u32, end: u32, name: String, common: bool }`；`SymbolEntry { text, name, keywords: Vec<String>, block: BlockId, emoji: bool }`。
- **frb 2.x 事实**：opaque 默认——`pub struct Api` + `impl Api` 的方法自动生成 Dart 类实例方法；同步方法标 `#[frb(sync)]`；`pub async fn` 在 Rust 侧需要 async 运行时 → 用"同步核心 + async 外壳"模式（Rust 单元测试直接测同步核心，不引入 tokio）。`char` 不支持 → 输入用 `String`，边界校验。tuple struct 不支持 → api.rs 用 plain struct/u16 转换（薄壳职责）。
- **安装 codegen**：`dart pub global activate flutter_rust_bridge_codegen`（Dart 托管，免 cargo 长编译）。
- **native-assets 后端**：`flutter_rust_bridge_codegen create app --integration-backend native-assets` 生成 app + `app/rust` crate + hook/build.dart；`flutter test` 时自动 cargo 构建主机库。若 `flutter test` 报 native-assets 未启用，先 `flutter config --enable-native-assets`。
- **GateGuard hook**：Edit/Write/Bash 被拦时陈述 4 事实（调用方；Glob/existing-file 检查；数据结构；用户指令原文）后重试。
- **禁碰**：`README.md` 有用户未提交修改（「参与贡献」节删除，Task 7 用 stash 流程保留）；`docs/alipay.png`、`docs/weixinpay.png` 是用户文件，任何任务不触碰。
- **提交规范**：conventional commits，无 `Co-Authored-By` trailer。

---

### Task 1: opi-ffi crate 骨架（cargo 侧）

**Files:**
- Create: `crates/opi-ffi/Cargo.toml`
- Create: `crates/opi-ffi/src/lib.rs`
- Create: `crates/opi-ffi/src/api.rs`
- Modify: `Cargo.toml`（workspace members）

- [ ] **Step 1: 写失败测试**

`crates/opi-ffi/src/api.rs`：

```rust
//! frb API 薄壳：类型转换与边界校验，内部持 engine_core::Engine。

use engine_core::Engine;

/// 引擎句柄（frb opaque，Dart 侧为 Api 类实例）。
pub struct Api {
    engine: Engine,
}
```

`crates/opi-ffi/src/lib.rs`：

```rust
pub mod api;
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo check -p opi-ffi`
Expected: FAIL — error[E0432]: unresolved import `engine_core`（crate 未创建/未入 workspace）。

- [ ] **Step 3: 创建 crate 并接线**

`crates/opi-ffi/Cargo.toml`：

```toml
[package]
name = "opi-ffi"
version = "0.1.0"
edition = "2024"

[dependencies]
engine-core = { path = "../engine-core" }
engine-data = { path = "../engine-data" }
flutter_rust_bridge = "2"
```

根 `Cargo.toml` members 增加 `"crates/opi-ffi"`。

- [ ] **Step 4: 运行确认通过**

Run: `cargo check -p opi-ffi && cargo clippy -p opi-ffi --all-targets -- -D warnings`
Expected: PASS，零警告。

- [ ] **Step 5: Commit**

```bash
git add Cargo.toml Cargo.lock crates/opi-ffi
git commit -m "feat(opi-ffi): crate skeleton with frb dep"
```

---

### Task 2: Flutter app 脚手架 + 模板验证 + crate 归位

**Files:**
- Create: `flutter/app/`（frb create 生成）
- Modify: `flutter/app/flutter_rust_bridge_codegen_config.yaml`（rust_input 路径）
- Modify: `crates/opi-ffi/Cargo.toml`（去掉 `[workspace]` 段、加 engine deps）
- Modify: `Cargo.toml`（members 已含 opi-ffi；若 create 生成的 crate 有独立 `[workspace]` 需移除）
- Create: `flutter/app/test/hello_test.dart`

- [ ] **Step 1: 安装 codegen 并创建脚手架**

```bash
dart pub global activate flutter_rust_bridge_codegen
cd /home/wwwroot/bag/opi && mkdir -p flutter && cd flutter
flutter_rust_bridge_codegen create app --integration-backend native-assets
```

Expected: 生成 `flutter/app/`（含 `rust/` crate、`lib/bridge_generated.dart`、`hook/build.dart`、`flutter_rust_bridge_codegen_config.yaml`、模板 `Greeter::greeting`）。若报 native-assets 相关错误：先 `flutter config --enable-native-assets` 再重试。

- [ ] **Step 2: 模板冒烟测试**

Run: `cd flutter/app && flutter test`
Expected: 模板自带的测试 PASS（验证 Flutter + 主机 Rust 构建 + FFI 全链路通）。

- [ ] **Step 3: crate 归位并接线**

```bash
git mv flutter/app/rust crates/opi-ffi
```

- 若 `crates/opi-ffi/Cargo.toml` 含 `[workspace]`（空段）：删除该段；补上 `engine-core`、`engine-data` path deps（Task 1 已写，保留，去掉模板示例依赖）。
- 根 `Cargo.toml` members 确认含 `"crates/opi-ffi"`（Task 1 已加）。
- 改 `flutter/app/flutter_rust_bridge_codegen_config.yaml` 的 `rust_input` 为 `../../crates/opi-ffi/src/api.rs`（相对 flutter/app）。
- 若 `hook/build.dart` 或其它文件引用 `rust/` 相对路径，同步改为 `../../crates/opi-ffi`。
- **若归位导致 native-assets 接线失败**（`flutter test` 无法找到 crate）：回退——保留 `flutter/app/rust`，在 Task 3-7 均在该路径工作，并在最终报告记录偏差。

- [ ] **Step 4: 写 hello 集成测试（验证归位后接线）**

`flutter/app/test/hello_test.dart`：

```dart
import 'package:app/bridge_generated.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('greeting round trip', () async {
    final api = await Greeter.create();
    expect(api.greeting('world'), 'Hello, world!');
    expect(api.greeting('世界'), 'Hello, 世界!');
    api.dispose();
  });
}
```

- [ ] **Step 5: 运行确认通过**

Run: `cd flutter/app && flutter test test/hello_test.dart`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add flutter/app crates/opi-ffi Cargo.toml Cargo.lock
git commit -m "feat(flutter): frb scaffold with native-assets backend, rust crate relocated"
```

---

### Task 3: api.rs 全量 API 面 + Rust 单元测试

**Files:**
- Modify: `crates/opi-ffi/src/api.rs`
- Modify: `crates/opi-ffi/src/lib.rs`（如需 re-export 类型）

- [ ] **Step 1: 写失败测试**

`crates/opi-ffi/src/api.rs` 追加 `#[cfg(test)]` 模块（先写测试，断言语义）：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn api() -> Api {
        Api::load_fallback_sync().expect("fallback load")
    }

    #[test]
    fn load_fallback_sync_ok() {
        let a = api();
        assert_eq!(a.buffer(), "");
        assert_eq!(a.mode(), ApiMode::Pinyin);
        assert!(a.learner_enabled()); // M1 默认开
    }

    #[test]
    fn pinyin_input_produces_candidates() {
        let mut a = api();
        a.input_key("w".into());
        a.input_key("o".into());
        assert_eq!(a.buffer(), "wo");
        let cands = a.candidates(3);
        assert_eq!(cands[0].text, "我");
        assert_eq!(cands[0].kind, ApiCandidateKind::Hanzi);
        assert!(cands[0].score > 0);
    }

    #[test]
    fn select_commits_and_records_learner() {
        let mut a = api();
        for c in ["w", "o"] { a.input_key(c.into()); }
        let text = a.select(0);
        assert_eq!(text, "我");
        assert_eq!(a.buffer(), "");
        assert!(a.export_user_words().contains("我"));
        a.remove_user_word("我".into());
        assert!(!a.export_user_words().contains("我"));
        a.clear_user_words();
        assert_eq!(a.export_user_words(), "[]");
    }

    #[test]
    fn input_key_boundary_rejects_non_single_char() {
        let mut a = api();
        assert_eq!(a.input_key("".into()), "");
        assert_eq!(a.input_key("ab".into()), "");
        assert_eq!(a.input_key("你".into()), ""); // 非 ASCII 也拒绝
        assert_eq!(a.buffer(), "");
    }

    #[test]
    fn select_out_of_range_returns_empty() {
        let mut a = api();
        for c in ["w", "o"] { a.input_key(c.into()); }
        assert_eq!(a.select(999), "");
    }

    #[test]
    fn mode_and_shift_and_space() {
        let mut a = api();
        a.switch_mode(ApiMode::English);
        for c in ["a", "b", "c"] { a.input_key(c.into()); }
        assert_eq!(a.buffer(), "abc");
        assert_eq!(a.input_space(), "abc");
        assert_eq!(a.buffer(), "");
        a.set_shift(true);
        a.input_key("a".into());
        assert_eq!(a.buffer(), "A");
        a.set_shift(false);
        a.switch_mode(ApiMode::Pinyin);
    }

    #[test]
    fn backspace_and_clear() {
        let mut a = api();
        a.input_key("w".into());
        a.backspace();
        assert_eq!(a.buffer(), "");
        a.input_key("w".into());
        a.clear();
        assert_eq!(a.buffer(), "");
    }

    #[test]
    fn symbol_search_and_blocks() {
        let a = api();
        let blocks = a.symbol_blocks();
        assert!(!blocks.is_empty());
        let hits = a.search_symbols("he".into());
        assert!(hits.iter().any(|s| s.text == "♥"));
        let b0 = blocks[0].clone();
        assert!(a.symbols_in_block(b0.id).len() > 0);
    }

    #[test]
    fn set_learner_toggles() {
        let mut a = api();
        a.set_learner(false);
        assert!(!a.learner_enabled());
        a.set_learner(true);
        assert!(a.learner_enabled());
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test -p opi-ffi`
Expected: FAIL — `Api::load_fallback_sync`、`ApiMode` 等不存在。

- [ ] **Step 3: 实现 api.rs 全量面**

`crates/opi-ffi/src/api.rs` 全文（替换模板内容；保持 Task 1 的 lib.rs 不变）：

```rust
//! frb API 薄壳：类型转换与边界校验，内部持 engine_core::Engine。

use engine_core::candidates::{Candidate, CandidateKind};
use engine_core::composer::Mode;
use engine_core::symbols::{Block, BlockId, SymbolEntry};
use engine_core::Engine;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ApiMode {
    Pinyin,
    English,
    Number,
    Symbol,
}

impl From<Mode> for ApiMode {
    fn from(m: Mode) -> Self {
        match m {
            Mode::Pinyin => ApiMode::Pinyin,
            Mode::English => ApiMode::English,
            Mode::Number => ApiMode::Number,
            Mode::Symbol => ApiMode::Symbol,
        }
    }
}

impl From<ApiMode> for Mode {
    fn from(m: ApiMode) -> Self {
        match m {
            ApiMode::Pinyin => Mode::Pinyin,
            ApiMode::English => Mode::English,
            ApiMode::Number => Mode::Number,
            ApiMode::Symbol => Mode::Symbol,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ApiCandidateKind {
    Hanzi,
    English,
    Emoji,
    Symbol,
}

impl From<CandidateKind> for ApiCandidateKind {
    fn from(k: CandidateKind) -> Self {
        match k {
            CandidateKind::Hanzi => ApiCandidateKind::Hanzi,
            CandidateKind::English => ApiCandidateKind::English,
            CandidateKind::Emoji => ApiCandidateKind::Emoji,
            CandidateKind::Symbol => ApiCandidateKind::Symbol,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ApiCandidate {
    pub text: String,
    pub kind: ApiCandidateKind,
    pub score: u64,
}

impl From<Candidate> for ApiCandidate {
    fn from(c: Candidate) -> Self {
        ApiCandidate { text: c.text, kind: c.kind.into(), score: c.score }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ApiBlock {
    pub id: u16,
    pub start: u32,
    pub end: u32,
    pub name: String,
    pub common: bool,
}

impl From<Block> for ApiBlock {
    fn from(b: Block) -> Self {
        ApiBlock { id: b.id.0, start: b.start, end: b.end, name: b.name, common: b.common }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ApiSymbolEntry {
    pub text: String,
    pub name: String,
    pub keywords: Vec<String>,
    pub block: u16,
    pub emoji: bool,
}

impl From<SymbolEntry> for ApiSymbolEntry {
    fn from(s: SymbolEntry) -> Self {
        ApiSymbolEntry { text: s.text, name: s.name, keywords: s.keywords, block: s.block.0, emoji: s.emoji }
    }
}

/// 引擎句柄（frb opaque）。同步核心 + async 外壳：Rust 测试测同步核心，Dart 侧 async 加载。
pub struct Api {
    engine: Engine,
}

impl Api {
    pub fn load_fallback_sync() -> Result<Api, String> {
        let dict = engine_data::fallback_dict();
        let symbols = engine_core::symbols::SymbolEngine::new();
        Ok(Api { engine: Engine::new(Box::new(dict), symbols, true) })
    }

    pub async fn load_fallback() -> Result<Api, String> {
        Self::load_fallback_sync()
    }

    pub fn load_sync(path: String) -> Result<Api, String> {
        let dict = engine_data::load_or_fallback(Some(std::path::Path::new(&path)))?;
        let symbols = engine_core::symbols::SymbolEngine::new();
        Ok(Api { engine: Engine::new(dict, symbols, true) })
    }

    pub async fn load(path: String) -> Result<Api, String> {
        Self::load_sync(path)
    }

    #[frb(sync)]
    pub fn input_key(&mut self, ch: String) -> String {
        let mut chars = ch.chars();
        let (Some(c), None) = (chars.next(), chars.next()) else {
            return String::new(); // 边界：拒绝空串/多字符
        };
        self.engine.input_key(c)
    }

    #[frb(sync)]
    pub fn backspace(&mut self) {
        self.engine.backspace();
    }

    #[frb(sync)]
    pub fn clear(&mut self) {
        self.engine.clear();
    }

    #[frb(sync)]
    pub fn switch_mode(&mut self, mode: ApiMode) {
        self.engine.switch_mode(mode.into());
    }

    #[frb(sync)]
    pub fn set_shift(&mut self, on: bool) {
        self.engine.set_shift(on);
    }

    #[frb(sync)]
    pub fn buffer(&self) -> String {
        self.engine.buffer().to_string()
    }

    #[frb(sync)]
    pub fn mode(&self) -> ApiMode {
        self.engine.mode().into()
    }

    #[frb(sync)]
    pub fn candidates(&self, limit: usize) -> Vec<ApiCandidate> {
        self.engine.candidates(limit).into_iter().map(Into::into).collect()
    }

    #[frb(sync)]
    pub fn select(&mut self, index: usize) -> String {
        self.engine.select(index)
    }

    #[frb(sync)]
    pub fn set_learner(&mut self, enabled: bool) {
        self.engine.set_learner(enabled);
    }

    #[frb(sync)]
    pub fn learner_enabled(&self) -> bool {
        self.engine.learner_enabled()
    }

    #[frb(sync)]
    pub fn remove_user_word(&mut self, text: String) {
        self.engine.remove_user_word(&text);
    }

    #[frb(sync)]
    pub fn clear_user_words(&mut self) {
        self.engine.clear_user_words();
    }

    #[frb(sync)]
    pub fn export_user_words(&self) -> String {
        self.engine.export_user_words()
    }

    #[frb(sync)]
    pub fn symbol_blocks(&self) -> Vec<ApiBlock> {
        self.engine.symbol_blocks().into_iter().map(Into::into).collect()
    }

    #[frb(sync)]
    pub fn symbols_in_block(&self, id: u16) -> Vec<ApiSymbolEntry> {
        self.engine.symbols_in_block(BlockId(id)).into_iter().map(Into::into).collect()
    }

    #[frb(sync)]
    pub fn search_symbols(&self, keyword: String) -> Vec<ApiSymbolEntry> {
        self.engine.search_symbols(&keyword).into_iter().map(Into::into).collect()
    }
}

#[cfg(test)]
mod tests {
    // …Task 3 Step 1 的测试原样…
}
```

注：`engine_core::candidates`、`engine_core::composer`、`engine_core::symbols` 均为 pub mod（M1 已确认）；若个别模块非 pub，改用 lib.rs re-export 的等价路径（以 `cargo check` 实际报错为准，仅调路径不改逻辑）。`#[frb(sync)]` 在纯 cargo 测试下为无操作宏，不影响编译。

- [ ] **Step 4: 运行确认通过**

Run: `cargo test -p opi-ffi && cargo clippy -p opi-ffi --all-targets -- -D warnings`
Expected: 全部 PASS，零警告。

- [ ] **Step 5: Commit**

```bash
git add crates/opi-ffi
git commit -m "feat(opi-ffi): full Engine API surface with sync core + async shell"
```

---

### Task 4: codegen 重新生成 + Dart 类型往返/错误映射测试

**Files:**
- Modify: `flutter/app/lib/bridge_generated.dart`（codegen 产物）
- Create: `flutter/app/test/api_roundtrip_test.dart`

- [ ] **Step 1: 写失败测试**

`flutter/app/test/api_roundtrip_test.dart`：

```dart
import 'package:app/bridge_generated.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('type round trips and error mapping', () async {
    final api = await Api.load_fallback();

    // 初始状态
    expect(api.buffer(), '');
    expect(api.mode(), ApiMode.pinyin);
    expect(api.learnerEnabled(), true);

    // String / usize→int / u64→int / 枚举 / 结构体往返
    api.inputKey('w');
    api.inputKey('o');
    expect(api.buffer(), 'wo');
    final cands = api.candidates(3);
    expect(cands.length, greaterThan(0));
    expect(cands.first.text, '我');
    expect(cands.first.kind, ApiCandidateKind.hanzi);
    expect(cands.first.score, greaterThan(0));

    // 错误映射：越界 select → 空串；非法输入 → 空串
    expect(api.select(999), '');
    expect(api.inputKey(''), '');
    expect(api.inputKey('ab'), '');
    expect(api.buffer(), 'wo');

    // 模式枚举映射
    api.switchMode(ApiMode.english);
    expect(api.mode(), ApiMode.english);
    api.switchMode(ApiMode.pinyin);

    // 符号：Vec<String> / 结构体字段
    final hits = api.searchSymbols('he');
    expect(hits.any((s) => s.text == '♥'), true);
    final blocks = api.symbolBlocks();
    expect(blocks, isNotEmpty);
    expect(blocks.first.common, isA<bool>());
    expect(api.symbolsInBlock(blocks.first.id), isNotEmpty);

    // 学习数据 JSON 往返
    api.clearUserWords();
    expect(api.exportUserWords(), '[]');
    api.setLearner(false);
    expect(api.learnerEnabled(), false);

    api.dispose();
  });
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd flutter/app && flutter test test/api_roundtrip_test.dart`
Expected: FAIL — `Api` 类不存在（codegen 产物仍是模板 Greeter）。

- [ ] **Step 3: 重新生成绑定**

Run: `cd flutter/app && dart run flutter_rust_bridge_codegen:generate`
Expected: `lib/bridge_generated.dart` 更新，出现 `Api`、`ApiMode`、`ApiCandidate`、`ApiSymbolEntry`、`ApiBlock` 类。若 `dart run ...:generate` 不可用（dev-deps 未含 codegen），改用 `flutter_rust_bridge_codegen generate`（Step 1 已全局激活）。

- [ ] **Step 4: 运行确认通过**

Run: `cd flutter/app && flutter test test/api_roundtrip_test.dart`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add flutter/app/lib/bridge_generated.dart flutter/app/test/api_roundtrip_test.dart
git commit -m "feat(flutter): regenerate frb bindings, type round-trip tests"
```

---

### Task 5: Dart 引擎流程集成测试

**Files:**
- Create: `flutter/app/test/engine_flow_test.dart`

- [ ] **Step 1: 写失败测试**

`flutter/app/test/engine_flow_test.dart`：

```dart
import 'package:app/bridge_generated.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('full pinyin flow: input, candidates, select, space', () async {
    final api = await Api.load_fallback();

    // 拼音输入 → 候选 → 选词提交
    for (final c in ['w', 'o']) {
      api.inputKey(c);
    }
    expect(api.candidates(8).first.text, '我');
    expect(api.select(0), '我');
    expect(api.buffer(), '');

    // 空格键：拼音模式选首候选
    for (final c in ['n', 'i']) {
      api.inputKey(c);
    }
    expect(api.buffer(), 'ni');
    api.inputKey(' '); // 空格 → 提交首候选
    expect(api.buffer(), '');

    api.dispose();
  });

  test('mode switching and english commit', () async {
    final api = await Api.load_fallback();
    api.switchMode(ApiMode.english);
    for (final c in ['a', 'b', 'c']) {
      api.inputKey(c);
    }
    expect(api.buffer(), 'abc');
    api.inputKey(' '); // 英文模式空格提交缓冲
    expect(api.buffer(), '');
    api.switchMode(ApiMode.pinyin);
    api.dispose();
  });

  test('symbols: block browse and keyword search', () async {
    final api = await Api.load_fallback();
    final blocks = api.symbolBlocks();
    expect(blocks, isNotEmpty);
    final all = <String>[];
    for (final b in blocks) {
      all.addAll(api.symbolsInBlock(b.id).map((s) => s.text));
    }
    expect(all, contains('♥'));
    expect(all, contains('😄')); // emoji 也在符号引擎内
    expect(api.searchSymbols('xin'), isNotEmpty);
    api.dispose();
  });

  test('custom dict load: bad path errors', () async {
    try {
      final api = await Api.load('/nonexistent/xx.opid');
      api.dispose();
      fail('should not load');
    } catch (e) {
      // load_or_fallback 对坏路径返回 Err → frb 抛异常
      expect(e, isA<AnyhowException>());
    }
  });
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd flutter/app && flutter test test/engine_flow_test.dart`
Expected: FAIL（`AnyhowException` 等以生成签名为准，微调后保持语义断言）。

- [ ] **Step 3: 按生成签名微调并运行**

Run: `cd flutter/app && flutter test test/engine_flow_test.dart`
Expected: PASS（若 `Api.load` 不存在——`load_sync` 带 Result 可能生成不同签名——以生成的 `Api.load` 实际签名为准调整调用；错误类型以生成的异常类为准替换 `AnyhowException`）。

- [ ] **Step 4: Commit**

```bash
git add flutter/app/test/engine_flow_test.dart
git commit -m "feat(flutter): engine flow integration tests"
```

---

### Task 6: EngineController（Riverpod 封装）

**Files:**
- Modify: `flutter/app/pubspec.yaml`（加 flutter_riverpod）
- Create: `flutter/app/lib/engine/engine_controller.dart`
- Create: `flutter/app/test/engine_controller_test.dart`
- Modify: `flutter/app/lib/main.dart`（最小化：仅保留 MaterialApp + 空页占位，M4 再接键盘）

- [ ] **Step 1: 加依赖**

Run: `cd flutter/app && flutter pub add flutter_riverpod`

- [ ] **Step 2: 写失败测试**

`flutter/app/test/engine_controller_test.dart`：

```dart
import 'package:app/engine/engine_controller.dart';
import 'package:app/bridge_generated.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('controller drives buffer/mode/candidates', () async {
    final ctrl = await EngineController.load();

    ctrl.input('w');
    ctrl.input('o');
    expect(ctrl.buffer, 'wo');
    expect(ctrl.candidates.length, greaterThan(0));
    expect(ctrl.candidates.first.text, '我');

    final committed = ctrl.select(0);
    expect(committed, '我');
    expect(ctrl.buffer, '');

    ctrl.backspace();
    expect(ctrl.buffer, '');

    ctrl.switchMode(ApiMode.english);
    expect(ctrl.mode, ApiMode.english);

    ctrl.dispose();
  });

  test('controller notifies listeners', () async {
    final ctrl = await EngineController.load();
    var notified = 0;
    ctrl.addListener(() => notified++);
    ctrl.input('w');
    expect(notified, greaterThan(0));
    ctrl.dispose();
  });
}
```

- [ ] **Step 3: 运行确认失败**

Run: `cd flutter/app && flutter test test/engine_controller_test.dart`
Expected: FAIL — `EngineController` 不存在。

- [ ] **Step 4: 实现 EngineController**

`flutter/app/lib/engine/engine_controller.dart`：

```dart
import 'package:flutter/foundation.dart';
import 'package:app/bridge_generated.dart';

/// 单一状态源：封装 opi-ffi Api，UI 经 notifyListeners 订阅。
/// M4 键盘接入时由 ChangeNotifierProvider 提供（Riverpod）。
class EngineController extends ChangeNotifier {
  EngineController._(this._api);

  final Api _api;

  String buffer = '';
  ApiMode mode = ApiMode.pinyin;
  List<ApiCandidate> candidates = const [];

  static Future<EngineController> load() async {
    final api = await Api.load_fallback();
    final ctrl = EngineController._(api);
    ctrl.refresh();
    return ctrl;
  }

  void refresh() {
    buffer = _api.buffer();
    mode = _api.mode();
    candidates = _api.candidates(8);
    notifyListeners();
  }

  void input(String ch) {
    _api.inputKey(ch);
    refresh();
  }

  void backspace() {
    _api.backspace();
    refresh();
  }

  void clear() {
    _api.clear();
    refresh();
  }

  String select(int index) {
    final text = _api.select(index);
    refresh();
    return text;
  }

  void switchMode(ApiMode m) {
    _api.switchMode(m);
    refresh();
  }

  void setShift(bool on) {
    _api.setShift(on);
    refresh();
  }

  List<ApiSymbolEntry> searchSymbols(String keyword) =>
      _api.searchSymbols(keyword);

  @override
  void dispose() {
    _api.dispose();
    super.dispose();
  }
}
```

`flutter/app/lib/main.dart` 替换为最小占位（`flutter_rust_bridge` 模板的 demo 计数页删掉）：

```dart
import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(home: Scaffold(body: SizedBox())));
}
```

- [ ] **Step 5: 运行确认通过**

Run: `cd flutter/app && flutter test && flutter analyze`
Expected: 全部 PASS，analyze 零告警。

- [ ] **Step 6: Commit**

```bash
git add flutter/app/pubspec.yaml flutter/app/pubspec.lock flutter/app/lib flutter/app/test/engine_controller_test.dart
git commit -m "feat(flutter): EngineController state wrapper with tests"
```

---

### Task 7: 收尾 —— 全量门禁、README、推送

**Files:**
- Modify: `README.md`

- [x] **Step 1: 全量门禁** —— 2026-08-12 完成（114 cargo tests + clippy 零警告 + 7 flutter tests + analyze 干净）

Run: `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cd flutter/app && flutter test && flutter analyze`
Expected: 全部 PASS，零警告零告警。

- [x] **Step 2: stash 保留用户 README 修改（沿用 M2 流程）** —— 2026-08-12 完成

```bash
git stash push -- README.md && git diff -- README.md   # 预期无输出（README 回到 HEAD）
```

- [x] **Step 3: README 更新** —— 2026-08-12 完成（实际两处：`opi-ffi/` 行已存在，仅加 `flutter/app/` 行 + 里程碑/当前阶段标记）

1) 仓库结构块：`opi-tools/` 行后加 `opi-ffi/` 行，并在 docs/superpowers 行后加 flutter 行：

```markdown
  opi-ffi/        # flutter_rust_bridge 绑定：Engine 全量 API，同步击键 + 异步加载（M3）
docs/superpowers/ # 设计规格与实施计划
data/             # 词库源数据（raw）与编译产物（generated，fallback.opid 入库）
flutter/app/      # Flutter 应用：EngineController（Riverpod）+ 集成测试（M3）
```

2) 里程碑行（M3）：

```markdown
- [x] **M3 FFI**：flutter_rust_bridge 绑定 + EngineController
```

3) 「当前阶段」行：

```markdown
> **当前阶段：M3 FFI 绑定 ✅ 完成（2026-08）**
```

- [x] **Step 4: 确认工作区干净** —— 2026-08-12 完成

Run: `git status`
Expected: 仅 README.md 与待提交内容；无 `data/generated/luna.opid`、无多余文件。

- [x] **Step 5: Commit 并恢复用户修改，推送** —— 2026-08-12 完成（3fcd6f2，push 成功）

```bash
git add README.md
git commit -m "docs: M3 FFI milestone complete, update README"
git stash pop
git diff -- README.md   # 预期仅显示用户的「参与贡献」节删除
git push origin main    # 用户已批准（计划即授权）
```

Expected: push 成功（origin = git@github.com:erikwang2013/opi.git，main）。

---

## Self-Review 记录

- **Spec 覆盖**：§2.1 opi-ffi 薄壳（Task 1/3）；§2.2 击键同步 + 加载 async（Task 3 的 `#[frb(sync)]` + async 外壳）；§2.3 单次同步调用（Dart 侧 `inputKey` 直接同步调用生成绑定）；§3.1 input_key/select/learner 流程（Task 5）；§4 EngineController Riverpod（Task 6）；§5.1 FFI 边界：UTF-8（frb 自带）+ 非法输入返回空串 + 永不 panic（Task 3 boundary 测试）；§5.2 opi-ffi Dart 集成测试：类型转换往返 + 错误码映射（Task 4）；§6 结构 `crates/opi-ffi/` + `flutter/app/`（Task 1/2）；M3 验收"Dart 集成测试通过"（Task 4/5）。
- **无占位**：所有步骤含完整代码/命令；失败→实现→通过的 TDD 链完整。
- **类型一致性**：`ApiMode/ApiCandidateKind`（Task 3 定义）在 Task 4/5/6 的 Dart 测试中使用；`Api.load_fallback`（Task 3）在 Task 4/5/6 使用；`EngineController.load`（Task 6）签名与其测试一致；`candidates(8)` 与 EngineController 内 `_api.candidates(8)` 一致。
- **已知风险与回退**：frb create/integrate 的 native-assets 接线在 crate 归位后若失效，回退到 `flutter/app/rust` 原地开发（Task 2 Step 3 记录偏差）；Task 5 的 `Api.load`/`AnyhowException` 以生成签名为准微调；`flutter test` 首次构建 Rust 库较慢（2-4 分钟）属预期。
- **用户文件保护**：Task 7 用 stash 流程保留 README 未提交修改；docs/*.png 全程不触碰。
