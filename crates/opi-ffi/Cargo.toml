[package]
name = "opi_ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
engine-core = { path = "../engine-core" }
engine-data = { path = "../engine-data" }
jni = "0.22"
serde_json = "1"
